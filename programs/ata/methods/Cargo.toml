[package]
name = "ata-methods"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[build-dependencies]
risc0-build = "=3.0.5"

[dependencies]
risc0-zkvm = { version = "=3.0.5", features = ["std"] }
ata_core = { path = "../core" }

[package.metadata.risc0]
methods = ["guest"]
